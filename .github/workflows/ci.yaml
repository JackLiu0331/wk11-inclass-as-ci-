name: CI
on:
  push:
    branches:
      - master

jobs:
  run-tests:
    name: Lint and Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install packages
        run: npm ci

      # Comment out the ESlint step
      # - name: Run ESlint
      #   run: npm run lint
